# Template — never commit real secrets. In production, provision these values
# via external-secrets / Vault / cloud secret manager and delete this file.
apiVersion: v1
kind: Secret
metadata:
  name: docuflow-secrets
  namespace: docuflow
type: Opaque
stringData:
  DATABASE_URL: "postgres://aeroxe:CHANGE_ME@postgres:5432/docu_flow?sslmode=require"
  JWT_SECRET: "CHANGE_ME_32_bytes_random_string_000"
  ENCRYPTION_KEY: "CHANGE_ME_44_char_base64_of_32_bytes"
  ADMIN_EMAIL: "admin@aeroxe.io"
  ADMIN_PASSWORD: "CHANGE_ME"
  S3_ACCESS_KEY: "CHANGE_ME"
  S3_SECRET_KEY: "CHANGE_ME"
